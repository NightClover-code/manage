import NavBar from '../NavBar';

const Header = () => {
  return (
    <header>
      <div className="custom__container">
        <NavBar />
      </div>
    </header>
  );
};

export default Header;
